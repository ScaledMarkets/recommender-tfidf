#
# Configurations pertaining to the build environment. The configuration below
# is unique to my personal Mac.
#

export maxerrs = 5

# Set Java version.

#export JavaVersion := 1.8
#export JDK := jdk1.8.0_161.jdk
export JavaVersion := 1.9
export JDK := jdk-9.0.4.jdk

# Define locations of external resources.

export HOME := $(shell echo ~)
export MVN := $(HOME)/Library/apache-maven-3.5.3/bin/mvn -t toolchains-mac.xml

export JAVA := /Library/Java/JavaVirtualMachines/$(JDK)/Contents/Home/bin/java
export JAVAC := /Library/Java/JavaVirtualMachines/$(JDK)/Contents/Home/bin/javac
export CDA_ROOT := $(HOME)/Projects/pf-cda-2.2.0
export JARCON_ROOT := $(HOME)/Projects/jarcon

# The following is only needed by maven:
export JDKToolLib := /Library/Java/JavaVirtualMachines/jdk1.8.0_161.jdk/Contents/Home/lib
export JAVA_HOME := /Library/Java/JavaVirtualMachines/jdk1.8.0_161.jdk/Contents/Home

# Define locations where we put jar files that are generated.

export jar_dir := $(HOME)/Transient/Recommenders
